# 攻略PDF生成器 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 编写 Python 脚本读取游戏 JSON 数据，生成完整的攻略 Markdown 文件，再用 Pandoc 转换为中文PDF。

**Architecture:** `guide/generate_guide.py` 作为主脚本，从项目根目录读取 `data/*.json`，拼接 `guide/templates/manual_content.py` 中的手写内容，输出 `guide/guide.md`；Pandoc + XeLaTeX 将 Markdown 转为 PDF。手写内容与自动生成内容分离，便于维护。

**Tech Stack:** Python 3 标准库（json, pathlib, os）、Pandoc、XeLaTeX（中文PDF）

---

## 文件结构

| 文件 | 职责 |
|------|------|
| `guide/generate_guide.py` | 主脚本：读取JSON → 拼接手写内容 → 输出 guide.md |
| `guide/templates/manual_content.py` | 所有手写内容（武将路线建议、危机策略、第一章文字） |
| `guide/guide.md` | 自动生成物，不手动编辑 |
| `guide/guide.pdf` | 最终产物，Pandoc 生成 |

---

## Task 1: 创建项目骨架与手写内容模块

**Files:**
- Create: `guide/templates/__init__.py`（空文件，使其成为包）
- Create: `guide/templates/manual_content.py`

- [ ] **Step 1: 创建目录结构**

```bash
mkdir -p "G:/TestGame/guide/templates"
```

- [ ] **Step 2: 创建空 `__init__.py`**

创建 `guide/templates/__init__.py`，内容为空。

- [ ] **Step 3: 创建 `guide/templates/manual_content.py`，写入全部手写内容**

创建文件 `guide/templates/manual_content.py`，内容如下：

```python
# manual_content.py — 所有手写内容（武将路线建议、危机策略、第一章说明）
# 自动生成脚本会直接引用此模块中的字符串常量。

# ── 封面 ──────────────────────────────────────────────────────────────────────

COVER = """---
title: "三国武将录 · 全攻略"
subtitle: "精华版 · 新手入门 + 全结局解锁指南"
date: "2026年"
---
"""

# ── 第一章：游戏入门 ──────────────────────────────────────────────────────────

CHAPTER_1 = """# 第一章：游戏入门

## 1.1 游戏概述

**三国武将录**是一款基于浏览器的三国文字冒险游戏。你扮演一位出身不同的人物，在乱世中做出抉择，最终达成 17 种结局之一。

游戏共分 **4 个阶段**：

| 阶段 | 名称 | 事件数 |
|------|------|--------|
| 第一阶段 | 少年期 | 5 + 1（角色专属） |
| 第二阶段 | 崛起期 | 6 + 1（角色专属） |
| 第三阶段 | 争霸期 | 6 + 1（角色专属） |
| 第四阶段 | 终局期 | 3 + 1（角色专属） |

每阶段结束时（少年期除外），可能触发**危机事件**——处理失败则直接死亡。

## 1.2 属性系统

### 可见属性（在界面中显示）

| 属性 | 含义 | 影响方向 |
|------|------|---------|
| 武 | 武力、战场生存能力 | 战斗相关结局、危机判定 |
| 智 | 谋略、判断力 | 谋士系结局、危机判定 |
| 德 | 品德、道义 | 仁义系结局、民心 |
| 魅 | 魅力、人际影响力 | 人望相关结局、危机判定 |

### 隐藏属性（不在界面显示，影响结局）

| 属性 | 含义 |
|------|------|
| 命运 | 综合运气，影响危机判定和部分结局 |
| 忠义 | 忠诚与义气，影响忠义系结局 |
| 魏 / 蜀 / 吴 | 三大阵营的倾向值，影响阵营系结局 |

> 所有属性值限制在 **[0, 100]** 范围内。

## 1.3 结局判定顺序

游戏结束时按以下顺序判定结局：

1. 若中途触发死亡（危机判定失败 / 选择即死选项），直接进入对应**死亡结局**
2. 若存活至终局，按顺序匹配**正常结局**，第一个满足条件者生效
3. 最后一种结局「平凡一生」无条件匹配，作为兜底

## 1.4 标记系统简介

部分选择会设置**标记（flag）**，解锁后续隐藏选项。例如：少年期「带乡亲撤离」会设置 `farmer_led_villagers` 标记，在崛起期解锁一个额外选项。详见附录。
"""

# ── 第二章：武将路线建议（手写部分）────────────────────────────────────────────
# key 与 characters.json 中的 id 一一对应

CHARACTER_TIPS = {
    "farmer": {
        "stars": "⭐⭐⭐（新手首选）",
        "recommended_endings": "忠臣良将 / 蜀汉柱石 / 平凡一生",
        "tip": (
            "初始属性偏低但均衡，容错率高，适合新手熟悉游戏。"
            "优先选择提升**德**和**忠义**的选项，避免德<28的抉择。"
            "少年期务必选「带乡亲撤离」，可在崛起期解锁高魅力选项（魅+10）。"
            "蜀汉路线：优先接受蜀阵营事件，保持忠义≥65、德≥70。"
        ),
    },
    "scholar": {
        "stars": "⭐⭐⭐⭐（智力流，推荐老玩家）",
        "recommended_endings": "幕后军师 / 权臣专政 / 魏国重臣",
        "tip": (
            "初始智力40，是所有武将中最高的，天生适合智谋路线。"
            "目标「幕后军师」需要智≥88、魅≥68、**武≤55**——全程避免提升武力选项。"
            "目标「权臣专政」需智≥82、魅≥75，同时德≤40、忠义≤32，走阴谋家路线。"
            "忠义初始48较高，走权臣路线需主动选择背信选项来压低忠义。"
        ),
    },
    "soldier": {
        "stars": "⭐⭐（武力流，高风险高回报）",
        "recommended_endings": "天下无双 / 战死沙场 / 忠臣良将",
        "tip": (
            "初始武38最高，但智力仅14，危机判定中「以智谋突围」几乎必死。"
            "目标「天下无双」需武≥90、智≤48，全程堆武力，彻底放弃智力选项。"
            "危机应对：优先选「武力判定」选项，或直接选「安全出口」保命。"
            "忠义初始偏低，不适合走忠义系结局，适合纯武力路线。"
        ),
    },
    "wanderer": {
        "stars": "⭐⭐⭐（均衡偏魅，适合社交流）",
        "recommended_endings": "乱世枭雄 / 东吴都督 / 归隐山林",
        "tip": (
            "魅力32是起点第二高，初始忠义仅30，天然适合走枭雄或东吴路线。"
            "目标「乱世枭雄」：武≥75、智≥75、德≤35，走强硬路线，不必顾虑德行。"
            "目标「归隐山林」：德≥75、命运≥62，且任意可见属性不能<30或>85（无极端属性）。"
            "命运初始45较高，是危机判定的天然优势，可走需要命运的路线。"
        ),
    },
    "merchant": {
        "stars": "⭐⭐⭐⭐⭐（最难，魅力流，路线独特）",
        "recommended_endings": "权臣专政 / 幕后军师 / 乱世枭雄",
        "tip": (
            "魅力38全角色最高，但武力仅14，战场危机极其危险，必须选安全出口。"
            "忠义初始仅20，天然适合走背信弃义路线（权臣专政：忠义≤32）。"
            "魏阵营初始55最高，适合走「魏国重臣」路线（魏≥80、智≥70）。"
            "**危机警告**：武14遇到「兵临绝境」，武力判定几乎必死，务必投降保命（安全出口）。"
        ),
    },
    "craftsman": {
        "stars": "⭐⭐⭐（德行流，稳健）",
        "recommended_endings": "忠臣良将 / 以身殉道 / 归隐山林",
        "tip": (
            "德30、忠义45是所有武将中最高的起点组合，天然适合忠义系结局。"
            "目标「以身殉道」：忠义≥88、命运≤40，全程堆忠义，接受命运下降的代价。"
            "目标「忠臣良将」：武≥68、德≥80、忠义≥75，需要适当提升武力。"
            "命运初始38偏低，危机判定时避免选「命运判定」选项，优先选武力或智力判定。"
        ),
    },
}

# ── 第三章：危机攻略建议（手写部分）────────────────────────────────────────────

CRISIS_TIPS = {
    "crisis_illness": {
        "tip": (
            "**最常见危机。** 若命运<30或武<25，务必选「将后事托付，安心养病」（安全出口）。"
            "拥有 `saved_doctor` 标记时会出现第四个选项，成功率极高，优先选择。"
            "平时注意留意能获得 `saved_doctor` 标记的事件。"
        )
    },
    "crisis_assassination": {
        "tip": (
            "武力高（≥30）的武将优先选「奋起反抗」；魅力高（≥15）的优先选「大声呼救」。"
            "商贾之子等武力极低的武将，直接选「从密道逃走」（安全出口），不要赌判定。"
        )
    },
    "crisis_battle": {
        "tip": (
            "武力判定门槛仅武≥8，大多数武将可通过。"
            "智力流武将（智≥40）可选「以智谋突围」，成功还有额外智力奖励。"
            "名声已损无所谓的路线（如枭雄路线）可直接「投降保命」，忠义-8但命运+10。"
        )
    },
    "crisis_execution": {
        "tip": (
            "此危机只在德行/忠义极低时触发，是「你走了坏路」的惩罚。"
            "智魅双高（智≥35且魅≥25）可选「慷慨陈词」，成功率高且有属性奖励。"
            "「认罪伏法」是最稳的安全出口，代价是武-5、魅-5，但忠义+10，适合想转型的玩家。"
        )
    },
}

# ── 第五章：全结局路线指南（手写部分）──────────────────────────────────────────
# key 与 endings.json 中的 id 一一对应

ENDING_ROUTES = {
    "unify": {
        "recommended_char": "寒门书生 或 行伍士卒（双线发展）",
        "route": (
            "最高难度结局，需武、智、魅同时达到极高水平。"
            "全程均衡发展，不能偏科。重点选择同时提升多属性的选项。"
            "阵营属性不影响此结局，可忽略魏/蜀/吴倾向。"
        ),
    },
    "wei_minister": {
        "recommended_char": "商贾之子（魏初始55）或 寒门书生",
        "route": (
            "保持魏阵营倾向值≥80，选择亲近曹魏势力的选项。"
            "同时确保蜀、吴倾向≤35，避免选择亲近蜀吴的事件。"
            "智力需达到≥70，全程接受智力奖励事件。"
        ),
    },
    "shu_guardian": {
        "recommended_char": "农夫之子（忠义42）或 匠人学徒（忠义45）",
        "route": (
            "保持蜀阵营倾向≥80，选择支持刘备/蜀汉的选项。"
            "德≥70 + 忠义≥65 是关键，全程走仁义路线。"
            "避免魏倾向提升（魏≤35），拒绝亲魏事件。"
        ),
    },
    "wu_admiral": {
        "recommended_char": "行伍士卒 或 江湖游侠",
        "route": (
            "保持吴阵营倾向≥80，选择效忠孙氏/东吴的选项。"
            "武≥60 + 智≥65 双线发展，纯武或纯智都无法达成此结局。"
            "魏≤35、蜀≤40，避免倒向其他阵营。"
        ),
    },
    "benevolent": {
        "recommended_char": "匠人学徒 或 农夫之子",
        "route": (
            "仁义路线巅峰。武、智、德三项均需极高，忠义也需≥72。"
            "全程选择道德高尚的选项，尤其不能选德行下降的事件。"
            "与「忠臣良将」路线类似但要求更高，适合已掌握基础的玩家。"
        ),
    },
    "strategist": {
        "recommended_char": "寒门书生（智40起点）",
        "route": (
            "核心约束：**武≤55**，全程不能提升武力。遇到武力提升选项，坚决放弃。"
            "智需达到≥88，这是全游戏最高的单项属性门槛，需几乎每次都选智力选项。"
            "魅≥68，配合高智发展，走谋士路线。"
        ),
    },
    "invincible": {
        "recommended_char": "行伍士卒（武38起点）",
        "route": (
            "核心约束：**智≤48**，全程放弃智力选项。"
            "武需达到≥90，是全游戏最高的单项武力要求。"
            "危机时只能选武力判定，绝对不能选智力判定（会提升智力）。"
        ),
    },
    "martyr": {
        "recommended_char": "匠人学徒（忠义45起点）",
        "route": (
            "忠义需达到≥88，同时命运≤40（低命运路线）。"
            "选择所有提升忠义的选项，包括代价较大的忠义事件。"
            "有意选择降低命运的选项（如某些冒险选择），保持命运值偏低。"
        ),
    },
    "loyal": {
        "recommended_char": "匠人学徒 或 农夫之子",
        "route": (
            "武≥68 + 德≥80 + 忠义≥75，三项均衡发展。"
            "适合新手第二局游玩，目标明确，路线清晰，容错率高。"
            "全程走正道，不做背信的选择。"
        ),
    },
    "chancellor": {
        "recommended_char": "寒门书生 或 商贾之子",
        "route": (
            "智≥82 + 魅≥75，同时德≤40、忠义≤32——需主动降低德行和忠义。"
            "走阴谋家路线：选择背叛、欺骗、利用他人的选项。"
            "忠义起点高的武将（如匠人学徒45）需要大量负面选择来压低忠义，难度极大。"
        ),
    },
    "warlord": {
        "recommended_char": "江湖游侠 或 行伍士卒",
        "route": (
            "武≥75 + 智≥75，同时德≤35——均衡但走强硬路线。"
            "不做仁义选择，以实力说话。适合喜欢「霸道」风格的玩家。"
            "注意德行的上限约束，避免选择提升德行的事件。"
        ),
    },
    "hermit": {
        "recommended_char": "江湖游侠（命运45）或 商贾之子（命运52）",
        "route": (
            "德≥75 + 命运≥62，且任意可见属性（武/智/德/魅）不低于30、不高于85。"
            "「无极端属性」是关键约束：不能偏科，保持四项属性均在中间范围。"
            "命运52起点的商贾之子有天然优势，但其武力仅14，需注意提升武力避免<30。"
        ),
    },
    "fallen": {
        "recommended_char": "行伍士卒",
        "route": (
            "武≥80 + 智≤50 + 忠义≥55，战死的武将路线。"
            "与「天下无双」类似但忠义有要求，走忠勇双全的武将路线。"
            "注意智力上限约束≤50，避免选智力选项。"
        ),
    },
    "bandit": {
        "recommended_char": "行伍士卒 或 江湖游侠",
        "route": (
            "武≥52 + 德≤28 + 命运≤32，是「道德败坏+命运不济」的组合。"
            "全程选择德行最低的选项，且有意选择降低命运的冒险选项。"
            "命运初始低的武将（行伍士卒30）更容易达成。"
        ),
    },
    "betrayed": {
        "recommended_char": "商贾之子（忠义20起点）",
        "route": (
            "德≤28 + 忠义≤28，双低路线——道德和忠义都要极低。"
            "全程选择最自私、最背信的选项。这是游戏对背叛者的「惩罚结局」。"
            "商贾之子忠义仅20，是最容易达成此结局的武将。"
        ),
    },
    "ordinary": {
        "recommended_char": "任意武将",
        "route": (
            "兜底结局，不满足任何其他条件时触发。"
            "第一局随意游玩，大概率以此结局结束，适合感受剧情。"
        ),
    },
}
```

- [ ] **Step 4: 提交**

```bash
cd G:/TestGame
git add guide/templates/__init__.py guide/templates/manual_content.py
git commit -m "feat: 添加攻略手写内容模块 manual_content.py"
```

---

## Task 2: 编写主生成脚本 `generate_guide.py`

**Files:**
- Create: `guide/generate_guide.py`

- [ ] **Step 1: 创建 `guide/generate_guide.py`**

```python
#!/usr/bin/env python3
# generate_guide.py — 读取游戏JSON数据，生成攻略 Markdown 文件
# 运行方式：在项目根目录执行 python guide/generate_guide.py

import json
from pathlib import Path

# 把 guide/ 加入路径，使 templates 包可导入
import sys
sys.path.insert(0, str(Path(__file__).parent))
from templates.manual_content import (
    COVER, CHAPTER_1,
    CHARACTER_TIPS, CRISIS_TIPS, ENDING_ROUTES,
)

ROOT = Path(__file__).parent.parent          # G:/TestGame/
DATA = ROOT / "data"
OUTPUT = Path(__file__).parent / "guide.md"


def load_json(filename):
    with open(DATA / filename, encoding="utf-8") as f:
        return json.load(f)


# ── 辅助：将 conditions 字典转为可读字符串 ─────────────────────────────────────

def conditions_to_str(conditions: dict) -> str:
    """把 endings.json 中的 conditions 转成人类可读的条件字符串。"""
    if not conditions:
        return "无（兜底结局）"
    parts = []
    for key, val in conditions.items():
        if key == "no_extreme":
            parts.append("任意可见属性 30≤x≤85")
        elif isinstance(val, dict):
            if "min" in val and "max" in val:
                parts.append(f"{key}≥{val['min']} 且 {key}≤{val['max']}")
            elif "min" in val:
                parts.append(f"{key}≥{val['min']}")
            elif "max" in val:
                parts.append(f"{key}≤{val['max']}")
    return "，".join(parts) if parts else "无"


# ── 辅助：根据结局条件评估难度星级 ──────────────────────────────────────────────

def ending_difficulty(ending: dict) -> str:
    """根据结局id给出难度星数（手动对应，确保准确）。"""
    difficulty_map = {
        "unify":        "★★★★★",
        "strategist":   "★★★★★",
        "invincible":   "★★★★",
        "wei_minister": "★★★★",
        "shu_guardian": "★★★",
        "wu_admiral":   "★★★★",
        "benevolent":   "★★★★★",
        "martyr":       "★★★★",
        "loyal":        "★★★",
        "chancellor":   "★★★★",
        "warlord":      "★★★",
        "hermit":       "★★★",
        "fallen":       "★★★",
        "bandit":       "★★",
        "betrayed":     "★★",
        "ordinary":     "★",
    }
    return difficulty_map.get(ending["id"], "★★★")


# ── 第二章：武将详解 ──────────────────────────────────────────────────────────

def attr_level(val: int) -> str:
    if val >= 35:
        return "高"
    if val >= 20:
        return "中"
    return "低"


def gen_chapter2(characters: list) -> str:
    lines = ["# 第二章：六位武将详解\n"]
    lines.append(
        "> 每局游戏只能选择一位武将。武将的初始属性决定了起点，"
        "但通过合理选择，任何武将都能达成大多数结局。\n"
    )
    for char in characters:
        icon = char.get("icon", "")
        name = char["name"]
        title = char["title"]
        attrs = char["attrs"]
        hidden = char["hidden"]
        cid = char["id"]
        tips = CHARACTER_TIPS.get(cid, {})

        lines.append(f"## {icon} {name}「{title}」\n")
        lines.append("| 属性 | 初始值 | 特点 |")
        lines.append("|------|--------|------|")
        for attr in ["武", "智", "德", "魅"]:
            v = attrs.get(attr, 0)
            lines.append(f"| {attr} | **{v}** | {attr_level(v)} |")
        lines.append("")
        fate = hidden.get("命运", "?")
        loyalty = hidden.get("忠义", "?")
        wei = hidden.get("魏", "?")
        shu = hidden.get("蜀", "?")
        wu = hidden.get("吴", "?")
        lines.append(
            f"**隐藏属性：** 命运 {fate} · 忠义 {loyalty} · "
            f"魏 {wei} · 蜀 {shu} · 吴 {wu}\n"
        )
        lines.append(f"**新手推荐指数：** {tips.get('stars', '⭐⭐⭐')}\n")
        lines.append(f"**推荐结局：** {tips.get('recommended_endings', '—')}\n")
        lines.append(f"**路线提示：** {tips.get('tip', '—')}\n")
        lines.append("---\n")
    return "\n".join(lines)


# ── 第三章：危机事件必读 ──────────────────────────────────────────────────────

CRISIS_EMOJI = {
    "crisis_illness":      "🤒",
    "crisis_assassination": "🗡️",
    "crisis_battle":       "⚔️",
    "crisis_execution":    "⛓️",
}

CRISIS_NAMES = {
    "crisis_illness":      "病入膏肓",
    "crisis_assassination": "月黑杀机",
    "crisis_battle":       "兵临绝境",
    "crisis_execution":    "秋后问斩",
}


def gen_chapter3(crisis_events: list) -> str:
    lines = ["# 第三章：危机事件必读\n"]
    lines.append(
        "> ⚠️ **危机事件**在每个阶段结束时可能触发（少年期免疫）。"
        "每种危机都有一个**安全出口**选项，代价较大但保证存活；"
        "其他选项需要属性判定，失败直接死亡。\n"
    )
    for crisis in crisis_events:
        cid = crisis["id"]
        emoji = CRISIS_EMOJI.get(cid, "⚠️")
        cname = CRISIS_NAMES.get(cid, crisis.get("title", cid))
        lines.append(f"## {emoji} {cname}\n")
        lines.append(f"**事件描述：** {crisis['description'][:80]}……\n")

        # 区分安全出口和判定选项
        safe_choices = []
        check_choices = []
        for c in crisis["choices"]:
            if "crisis_check" in c:
                check_choices.append(c)
            else:
                safe_choices.append(c)

        if safe_choices:
            sc = safe_choices[0]
            effects = sc.get("effects", {})
            eff_str = "，".join(
                f"{k}{'+' if v > 0 else ''}{v}" for k, v in effects.items()
            )
            lines.append(f"**✅ 安全出口：**「{sc['text']}」")
            lines.append(f"  → 代价：{eff_str if eff_str else '无'}\n")

        if check_choices:
            lines.append("**🎲 判定选项：**")
            for cc in check_choices:
                check = cc.get("crisis_check", {})
                check_str = conditions_to_str(check)
                death = cc.get("death_ending", "死亡")
                lines.append(f"  - 「{cc['text']}」（需 {check_str}）")
                lines.append(f"    失败结果：触发「{death}」死亡结局")

        tip_data = CRISIS_TIPS.get(cid, {})
        if tip_data.get("tip"):
            lines.append(f"\n**💡 攻略建议：** {tip_data['tip']}\n")

        lines.append("---\n")
    return "\n".join(lines)


# ── 第四章：结局图鉴 ──────────────────────────────────────────────────────────

def gen_chapter4(endings_data: dict) -> str:
    endings = endings_data["endings"]
    lines = ["# 第四章：结局图鉴\n"]
    lines.append(
        "> 共 **17 种结局**（4 种死亡 + 13 种正常）。"
        "正常结局按顺序匹配，满足条件的第一个生效。\n"
    )

    # 正常结局
    lines.append("## 正常结局（13种）\n")
    lines.append("| 结局 | 图标 | 关键解锁条件 | 难度 |")
    lines.append("|------|------|------------|------|")
    for e in endings:
        if e.get("isDeath"):
            continue
        icon = e.get("icon", "📜")
        cond = conditions_to_str(e.get("conditions", {}))
        diff = ending_difficulty(e)
        lines.append(f"| {e['name']} | {icon} | {cond} | {diff} |")

    lines.append("")

    # 死亡结局
    lines.append("## 死亡结局（4种）\n")
    lines.append("| 结局 | 图标 | 触发方式 |")
    lines.append("|------|------|---------|")
    death_triggers = {
        "death_illness":      "触发「病入膏肓」危机且判定失败",
        "death_assassinated": "触发「月黑杀机」危机且判定失败",
        "death_battle":       "触发「兵临绝境」危机且判定失败，或选择即死选项",
        "death_executed":     "触发「秋后问斩」危机且判定失败",
    }
    for e in endings:
        if not e.get("isDeath"):
            continue
        icon = e.get("icon", "💀")
        trigger = death_triggers.get(e["id"], "危机判定失败")
        lines.append(f"| {e['name']} | {icon} | {trigger} |")

    lines.append("")
    return "\n".join(lines)


# ── 第五章：全结局路线指南 ────────────────────────────────────────────────────

def gen_chapter5(endings_data: dict) -> str:
    endings = endings_data["endings"]
    lines = ["# 第五章：全结局路线指南\n"]
    lines.append(
        "> 本章为高玩提供每个结局的详细路线建议。"
        "死亡结局通过危机判定失败触发，不单独列出路线。\n"
    )
    for e in endings:
        if e.get("isDeath"):
            continue
        eid = e["id"]
        icon = e.get("icon", "📜")
        route_data = ENDING_ROUTES.get(eid, {})
        cond = conditions_to_str(e.get("conditions", {}))

        lines.append(f"## {icon} {e['name']}\n")
        lines.append(f"**墓志铭：** *{e.get('epitaph', '')}*\n")
        lines.append(f"**解锁条件：** {cond}\n")
        lines.append(f"**推荐武将：** {route_data.get('recommended_char', '—')}\n")
        lines.append(f"**路线建议：** {route_data.get('route', '—')}\n")
        lines.append("---\n")
    return "\n".join(lines)


# ── 附录：标记系统 ────────────────────────────────────────────────────────────

def gen_appendix(event_files: list) -> str:
    """扫描所有事件文件，收集 set_flags 和 requires_flags 的使用情况。"""
    # 已知关键标记（手动维护，确保说明准确）
    known_flags = {
        "farmer_led_villagers": {
            "trigger": "农夫之子·少年期·选「召集村民带大家撤离」",
            "effect": "崛起期解锁「率领旧日村民组建义军核心」（魅+10）",
        },
        "joined_coalition": {
            "trigger": "少年期·虎牢关事件·选「冷静观察战局，分析各路诸侯」",
            "effect": "后续联盟相关事件解锁",
        },
        "saved_doctor": {
            "trigger": "特定事件中救治神医（需留意事件描述）",
            "effect": "病殁危机新增高成功率选项",
        },
    }

    lines = ["# 附录：标记系统说明\n"]
    lines.append(
        "> **标记（Flag）** 是游戏中的隐藏状态，由特定选择触发，"
        "解锁后续的隐藏选项。以下是已知的关键标记。\n"
    )
    lines.append("| 标记名 | 触发条件 | 解锁效果 |")
    lines.append("|--------|---------|---------|")
    for flag, info in known_flags.items():
        lines.append(f"| `{flag}` | {info['trigger']} | {info['effect']} |")

    lines.append("")
    lines.append(
        "> **提示：** 游戏中还存在其他标记，部分仅影响事件描述文本。"
        "建议多次游玩，探索不同选择带来的变化。\n"
    )
    return "\n".join(lines)


# ── 主函数 ────────────────────────────────────────────────────────────────────

def main():
    print("📖 正在读取游戏数据...")
    characters = load_json("characters.json")
    endings_data = load_json("endings.json")
    crisis_events = load_json("events-crisis.json")
    # 收集所有事件文件（附录用）
    event_files = list(DATA.glob("events-*.json"))

    print("✍️  正在生成攻略内容...")
    sections = [
        COVER,
        CHAPTER_1,
        gen_chapter2(characters),
        gen_chapter3(crisis_events),
        gen_chapter4(endings_data),
        gen_chapter5(endings_data),
        gen_appendix(event_files),
    ]

    output = "\n\n".join(sections)
    OUTPUT.write_text(output, encoding="utf-8")
    print(f"✅ 攻略已生成：{OUTPUT}")
    print()
    print("📄 转换为PDF（需安装Pandoc + XeLaTeX）：")
    print(
        f"pandoc {OUTPUT} -o guide/guide.pdf "
        "--pdf-engine=xelatex "
        '-V mainfont="SimSun" '
        "-V geometry:margin=2cm "
        "--toc --toc-depth=2"
    )


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 从项目根目录运行脚本，验证输出**

```bash
cd G:/TestGame
python guide/generate_guide.py
```

预期输出：
```
📖 正在读取游戏数据...
✍️  正在生成攻略内容...
✅ 攻略已生成：G:\TestGame\guide\guide.md
```

如果报错 `ModuleNotFoundError`，确认运行目录是 `G:/TestGame`，不是 `guide/`。

- [ ] **Step 3: 检查生成的 `guide/guide.md` 内容**

打开 `guide/guide.md`，验证：
- 封面存在（含标题和副标题）
- 第一章有属性表格
- 第二章有6个武将卡片，每个都有属性表格和路线提示
- 第三章有4种危机，每种都有安全出口和判定选项
- 第四章有完整的17种结局速查表（13正常+4死亡）
- 第五章有13种正常结局的路线指南
- 附录有标记表格

- [ ] **Step 4: 提交**

```bash
cd G:/TestGame
git add guide/generate_guide.py guide/guide.md
git commit -m "feat: 添加攻略生成脚本，生成完整攻略Markdown"
```

---

## Task 3: 转换为 PDF

**Files:**
- 生成: `guide/guide.pdf`（Pandoc 输出，不纳入版本控制）

- [ ] **Step 1: 确认 Pandoc 和 XeLaTeX 已安装**

```bash
pandoc --version
xelatex --version
```

如果未安装：
- Pandoc：https://pandoc.org/installing.html
- XeLaTeX：安装 MiKTeX（Windows）或 TeX Live（跨平台）

- [ ] **Step 2: 检测系统可用的中文字体**

```bash
# Windows
fc-list :lang=zh
```

常见可用字体：`SimSun`（宋体）、`Microsoft YaHei`（微软雅黑）、`SimHei`（黑体）

- [ ] **Step 3: 运行 Pandoc 转换**

```bash
cd G:/TestGame
pandoc guide/guide.md -o guide/guide.pdf ^
    --pdf-engine=xelatex ^
    -V mainfont="SimSun" ^
    -V CJKmainfont="SimSun" ^
    -V geometry:margin=2cm ^
    --toc ^
    --toc-depth=2
```

> Windows 命令行用 `^` 换行；PowerShell 用 `` ` ``；Linux/Mac 用 `\`。

预期输出：无报错，`guide/guide.pdf` 文件生成。

- [ ] **Step 4: 检查PDF**

打开 `guide/guide.pdf`，确认：
- 中文正常显示（无乱码、无方框）
- 目录页存在，链接正确
- 表格对齐
- 6个武将卡片格式一致
- 17种结局全部列出

- [ ] **Step 5: 若中文乱码，尝试备用字体**

```bash
pandoc guide/guide.md -o guide/guide.pdf ^
    --pdf-engine=xelatex ^
    -V mainfont="Microsoft YaHei" ^
    -V CJKmainfont="Microsoft YaHei" ^
    -V geometry:margin=2cm ^
    --toc ^
    --toc-depth=2
```

- [ ] **Step 6: 将 PDF 生成命令写入便捷脚本**

创建 `guide/build_pdf.bat`（Windows 批处理）：

```bat
@echo off
echo 正在生成攻略Markdown...
python generate_guide.py
echo 正在转换PDF...
pandoc guide.md -o guide.pdf ^
    --pdf-engine=xelatex ^
    -V mainfont="SimSun" ^
    -V CJKmainfont="SimSun" ^
    -V geometry:margin=2cm ^
    --toc ^
    --toc-depth=2
echo 完成！攻略已保存为 guide.pdf
pause
```

- [ ] **Step 7: 将 guide.pdf 加入 .gitignore，提交脚本**

```bash
cd G:/TestGame
echo guide/guide.pdf >> .gitignore
git add guide/build_pdf.bat .gitignore
git commit -m "feat: 添加PDF构建脚本和.gitignore"
```

---

## 自查：规格覆盖检查

| 规格要求 | 对应 Task | 状态 |
|---------|-----------|------|
| 封面 | Task 2（COVER 常量） | ✅ |
| 第一章：游戏入门，属性说明，流程 | Task 1（CHAPTER_1）| ✅ |
| 第二章：6武将属性表+路线建议 | Task 2（gen_chapter2）| ✅ |
| 第三章：4种危机+安全出口+策略 | Task 2（gen_chapter3）| ✅ |
| 第四章：17种结局速查表（全部列出）| Task 2（gen_chapter4）| ✅ |
| 第五章：13种正常结局路线指南 | Task 2（gen_chapter5）| ✅ |
| 附录：标记系统说明 | Task 2（gen_appendix）| ✅ |
| no_extreme 条件人类可读 | Task 2（conditions_to_str）| ✅ |
| 兜底结局（空conditions）处理 | Task 2（conditions_to_str 返回"无（兜底结局）"）| ✅ |
| Python 仅标准库 | Task 2（仅 json, pathlib, sys）| ✅ |
| Pandoc + XeLaTeX 中文PDF | Task 3 | ✅ |
| guide.pdf 不纳入版本控制 | Task 3 Step 7 | ✅ |
